version: '3.8'

services:
  glyphforge:
    build: .
    container_name: glyphforge-app
    ports:
      - "3001:3001"
      - "5173:5173"
    volumes:
      - D:/Glyphforge-data/prompt:/data/prompt
    environment:
      - NODE_ENV=production
    restart: unless-stopped
